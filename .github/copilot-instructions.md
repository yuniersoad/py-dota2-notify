# Project Management with uv

This project uses `uv` for dependency management and running tasks.

- To install dependencies, run: `uv pip install -e .[dev]`
- To run tests, use: `uv run pytest`

# Asyncio Usage

Use `asyncio` with `async` and `await` for all external calls.
